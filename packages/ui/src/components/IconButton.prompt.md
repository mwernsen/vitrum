Circular icon-only button for toolbars and chrome; always pass `label`.

```jsx
<IconButton label="Close" variant="ghost"><XIcon size={18}/></IconButton>
```

`dark` variant for the studio's dark canvas chrome.
